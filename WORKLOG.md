# Work Log

## Completed

- **096** — Manual annotation sidecar notes: new `src/marketatlas/visualization/notes.py` — `note_path` (extension swap), `read_note` (None = not reviewed, empty = reviewed/no action), `iter_notes` (deterministic PNG × note ordering), `write_note` (tests/scaffolding only; empty template drops a review-ready `.txt`). `render_poi_snapshots` gains `write_notes` flag; both `run --snapshots` and `snapshot` subcommands get a `--notes` flag to drop empty `.txt` templates beside each PNG (default off, no filesystem noise). Note files live under ignored `output/` so never git-tracked. JSON structured output untouched. Tests: `tests/test_notes.py` (extension swap, missing/empty/content discrimination, ordering, idempotent template, wording preserved, render with/without templates) + CLI `--notes` threading in `test_snapshot_cli.py`.

- **095** — Snapshot kind filter on CLI: new `--kinds` flag on both `run --snapshots` and `snapshot` subcommand (comma-separated `trade,rejection,pattern,sr,swing`, `!x` negation). Added `parse_kinds()`/`VALID_SNAPSHOT_KINDS` in `cli/commands.py` (invalid kind → clear error, exit 1). `locate_pois` gains `exclude_kinds` param + `!x` handling; `render_poi_snapshots` threads both through. Tests: `tests/test_snapshot_cli.py` (parse, mock-thread, invalid-kind, missing-json) + renderer include/exclude test. 280 tier1 pass, lint clean.

- **094** — Parallel instrument analysis: `PortfolioBacktester` uses `ThreadPoolExecutor` for per-instrument `graph.run_with_evidence` + `evaluate_all_with_rejections` in inner loop. Module-level `_analyze_instrument` function (pickle-safe). Pool size configurable via `pool_size` param, defaults to `min(instruments, cpu_count)`. Single-instrument path unchanged. Output identical to serial execution. 1315 tests pass, lint clean.
- **093** — MarketView computed property cache: `history`, `prices`, `highs`, `lows`, `volumes`, `timestamps` now lazy-cache via `object.__setattr__` on frozen dataclass slots. Each property computes once per `MarketView` instance. Eliminates thousands of throwaway tuples per backtest (9554 frames × 9 analyzers). 1315 tests pass, lint clean.
- **084a** — Signal/Risk requires(): `Signal` ABC gains abstract `requires() -> tuple[FactKey, ...]` and default `produces()` (empty). `PullbackSignal.requires()` returns `("pullback_pattern", "trend", "atr_14")`. `RiskEngine` gains `requires()` returning `("atr_14", "sr", "swing")` and `produces()` (empty). Enables 084b contract derivation. 236 tier1 tests pass, lint clean.
- **084b** — Signal/risk contracts: contracts already derived correctly by `_derive_contract` via `_register_metadata` (no code change needed). Added 2 acceptance criteria tests verifying `registry.contract("generate_signal")` → `("pullback_pattern", "trend", "atr_14")` and `registry.contract("manage_risk")` → `("atr_14", "sr", "swing")`. 238 tier1 tests pass.
- **084c** — Compiler completeness check: new `CompletenessPass` (stage 1, post-registry-resolution) verifies every contract-declared analyzer input is supplied as a `ReferenceExpression` param. Skips non-analyzer providers (signal/risk handle deps at runtime via lowering), skips definitions with no ref params (builder API path). Uses `_is_reference_param` for `ChoiceExpression` leaf detection. Fixed `CompilationError` frozen dataclass `__traceback__` assignment via post-hoc `__setattr__` override. Updated 8 test DSL snippets with complete bindings; 2 tests updated to expect `CompilationError` where CompletenessPass now catches before `UnsatisfiedDependencyError`. 1316 tests pass.
- **084d** — TrendAnalyzer requires atr_14: `requires()` returns `(ema_20, ema_50, atr_14)`. Removed optional `facts.get` fallback — ATR is now a hard dependency. Updated `strategy/swing.dsl` trend node with `atr_14: atr_14`. CompletenessPass now requires all inputs as explicit refs when any are (so trend defs in builder API tests also gained ema_20/ema_50 refs + ATRAnalyzer definitions). Fixed `test_strength_scales_with_spread` (gentle_rise step reduced to 0.1 so spread/ATR stays below 1.0). Updated contract test, sorted analyzer lists, config counts, and cross-TF assertion tuples across 15+ test files. 1316 tests pass.
- **084e** — PullbackSignal DSL-injected keys: constructor gains `bindings: dict[str, str] | None = None` param (follows RiskEngine pattern). `evaluate()` resolves keys via `self._bindings.get("pullback_pattern", self._pullback_key)` etc., so compiler-injected keys from `SignalConfig.rules["bindings"]` (lowering.py:261-262) are consumed instead of ignored. Programmatic construction defaults unchanged. No new tests needed — existing signal contract tests already cover both paths; builder equivalence test (`test_full_literal_matches_builder_graph`) passes. 1316 tests pass.
- **087** — Signal registry: created `analysis/signals/registry.py` with `SIGNAL_TYPES` dict mapping signal class names to classes; `Strategy._build_signals()` now imports from registry instead of hardcoding `PullbackSignal`; AST `create_default_registry()` derives signal class from `SIGNAL_TYPES` instead of direct import. Adding a new signal requires: new file + one registry line. 65 tests pass, lint clean.
- **092** — Data provider plugin system: new `providers/registry.py` (register/get/names), providers self-register on import via `register("name", Class)` at module bottom, `cli._make_provider` falls back to `registry.get()` for unknown providers (known providers still use concrete imports for backward compat with test mocks). 89 tests pass (1 pre-existing env failure untouched).
- **088** — Test pruning: removed `test_dsl_lexer.py` (36 tests — parser tests cover token semantics), `test_html_output_validation.py` (31 tests — fragile HTML structure checks), `test_interactive_renderer.py` (69 tests — JS test suite covers rendering). Trimmed `test_ast_golden_expansion.py` from 13→3 representative tests (single choice, nested, cartesian product). Added 5 smoke tests to `test_html_renderer.py` (title, chart div, script block, JS constants, no unresolved placeholders). 1451→1310 tests, −2461 lines. 1 pre-existing default-registry env failure untouched.
- **085** — Split `analysis/ast/pipeline.py` (939 lines) into three modules: `expansion.py` (181 lines, choice expansion + helpers), `lowering.py` (338 lines, IR lowering + fact-key compilation), `pipeline.py` (504 lines, thin orchestrator + CompilerPass classes). Public API unchanged — all existing `from ...pipeline import ...` paths re-export. No circular imports. 1415 tests pass.

- **082** — A/B index controls: `render_ab_index` now progressively enhances the 081 page with a control bar (one `<select>` per choice dimension, values sorted deterministically via `_option_sort_key`). Variant summaries embedded once as `AB_VARIANTS`/`AB_DIMS` JSON (`_ab_variant_json`/`_ab_dims_json`, built from the same `TradeBook.summary` the server-rendered cells use; ident values stored as their rendered strings); a single hand-written inline `_AB_INDEX_JS` block (mirroring the `InteractiveRenderer` `// @data:` placeholder + IIFE style, no framework/bundler) rebuilds the `#ab-grid-body` rows from JSON with formatting identical to the Python `_fmt_*`/`_sign_class` helpers and shows/hides `data-slug`-tagged `section.variant` details, updating the `#ab-count` readout; controls default to "All" so the initial view equals the 081 static page. Progressive enhancement: with JS off the full static tables remain server-rendered and the JSON is purely additive; JSON is `</`-escaped for inline-script safety. 7 new tests (`TestABIndexControls`: JSON matches server-rendered grid cells incl. sign classes/PF-∞, static row count vs JSON, one select per dim with All+distinct values, data-slug on sections, no-JS static fallback intact, `node --check` syntax, and a `node` smoke test running the script under a minimal DOM stub that drives a select to narrow grid+detail sections and reset to All). Fixed stale 081 `test_grid_full_metrics` expectations (+2.00 → +20.00) left over from the 2c15699 tradebook re-size change. 1449 total (incl. 1 pre-existing default-registry env failure).

- **081** — A/B comparison index: `render_ab_index(rows, output_dir, *, chart_name, instruments)` in `visualization/portfolio.py` writes a static `ab.html` at the 080 output-tree root (`out/ab.html` for `--output out.html`). Summary grid = one row per choice combination; choice columns come from `variant_columns(identities)` (new `analysis/ast/variant.py` helper returning `(varying_keys, headers, slugs)` from bare variant identities, sharing the 079/080 varying-dimension + bare-name-collision logic) followed by Trades/W-L/Win rate/P&L/Return/Max DD/Profit factor/Expectancy straight from `TradeBook.summary` (no recompute). Per-variant detail sections reuse the 077 `_instrument_row`/`_strategy_rows_html`/`_sign_class`/`_fmt_*` markup; each instrument row links to its per-variant chart via relative href (`ms010/portfolio.A.html` portfolio, `p20/out.html` single-symbol), so the page opens from disk with no server and no JS. Index-page CSS extracted into shared `_INDEX_CSS` (portfolio + AB templates format from one style block). `cli._render_ab_variants` (both `--ab` paths) writes the index alongside the chart tree; single-symbol passes its chart filename + instrument; portfolio path defaults from `PortfolioBacktestResult.instruments`. 8 new tests (renderer unit tests over a two-variant fixture asserting grid rows/metrics, per-variant detail tables, and ms010/ms099 hrefs; `variant_columns` unit tests incl. cartesian dims + qualified shared-bare-name headers + lone-variant default; CLI single-symbol + portfolio output-tree tests assert the index page and its links). 1435 total (incl. 1 pre-existing default-registry failure).

- **080** — A/B output tree: `variant_slugs(templates)` (shared `_varying`/`_bare_counts` identity logic with `variant_labels`) produces deterministic, ordinal-free, filesystem-safe slugs keyed by each variant's chosen values (`min_strength=0.5` → `ms050`, `max_stop_atr=3` → `atr3`, multi-dim `p20_ms030`; unknown param names fall back to their full sanitized name; lone variant → `default`). `cli._render_ab_variants` (shared by both `--ab` paths, no duplicated chart-write code) writes the tree `--output out.html` → `out/<slug>/out.html` for single-symbol and `out/<slug>/portfolio.<canonical>.html` per instrument (reusing `render_per_instrument_charts`) for portfolio `--ab`; non-`--ab` run output unchanged. 11 new tests (slug unit tests incl. cartesian combos, determinism, filesystem-safety, collision-freedom, label-shared dims, unknown-param + string-timeframe sanitization; CLI single-symbol + portfolio tree-layout tests). 1422 total.

- **079** — Portfolio A/B test runner: `run --ab --instruments <portfolio.yaml>` expands the DSL's `<a | b | c>` choices via `ASTCompiler.compile_templates` and runs one `PortfolioBacktester` per concrete variant, each on a fresh `StrategyBundle`/`TradeBook` (no cross-variant state), printing a comparison row per variant (trades, W/L, P&L, return, drawdown, PF, expectancy). New `analysis/ast/variant.py` generalizes variant identity to every node type — analyzer params, signal rules, risk params (`variant_identity`, keys `{node}.{param}`/`risk.{param}`, `bindings`/nested containers excluded) — and `variant_labels` names each variant's chosen values (bare `param=value` per varying dimension in sorted-key order, fully-qualified only on name clash; single choice-free variant → `default`). Shared fetch/store setup extracted into `_load_ab_stores` (used by both single-symbol and portfolio `--ab`); `_ab_row` prints the 7-metric line; `_compile_ab_templates` requires `.dsl` (`.yaml` under `--ab` errors cleanly). 13 new tests (variant-identity/label unit tests incl. analyzer/signal/risk choice + cartesian ordering + default; CLI flag wiring; mocked choice-free single variant; real two-instrument/two-value-choice fixture asserting 2 variants, labels, 1-vs-0 trades, and distinct tradebook objects). 1409 total.

- **076** — CLI portfolio run path: `run --instruments <file>` now executes the full portfolio path end-to-end (069 data load → `PortfolioBacktester` 075 → summary output). After the `PORTFOLIO DATA` block it builds one `Strategy`/`StrategyBundle`, runs the backtester over the merged primary-timeframe calendar with a progress bar, prints `PORTFOLIO RESULTS` (shared balance, total P&L, return, win/loss counts, win rate, max drawdown, profit factor) plus `By instrument:` and `By strategy:` breakdowns straight from the shared book's `summary`. `--output` names the index page; per-instrument charts are derived from its stem via `render_portfolio` (077), skipped when `--output` is empty. Per-instrument data-load failures stay non-fatal (reported, remaining instruments continue); `--pickle` dumps the picklable `PortfolioBacktestResult`. Existing portfolio CLI tests extended with a mocked backtest pipeline (`Strategy`/`StrategyBundle`/`PortfolioBacktester`/`render_portfolio`) so they drive data loading + summary output; new end-to-end test asserts backtest wiring (window/hold), summary + by-instrument/by-strategy output, and `render_portfolio` stem derivation. 1 new test. 1381 total.

- **077** — Portfolio visualization: new `visualization/portfolio.py` — `render_per_instrument_charts` reuses `InteractiveRenderer` with `TradeBook.filtered_by_instrument(canonical)` (new: copy of the shared book holding only that instrument's trades, balance/peak recomputed) so trade markers + summary bar reflect only that instrument; chart title carries the canonical name, files `{stem}.{canonical}.html`. `render_portfolio_index` writes a self-contained static `{stem}.html` (no charting lib): shared-book summary bar (balance, total P&L, return, drawdown, trade counts, expectancy) + one row per instrument (P&L/trades/W-L/win rate/PF) + optional by-strategy table, each instrument linking via relative href so the page works from disk. `render_portfolio` does both. `TradeBook` by-instrument/by-strategy breakdowns now include `trades`/`gross_profit`/`gross_loss`/`win_rate`/`profit_factor` (fed straight from `summary` — no recompute); trade JSON gains `instrument` field. 10 new tests across `test_interactive_renderer` (per-instrument TRADES arrays + summary scoping + titles) and `test_html_output_validation` (index rows/hrefs/summary). 1387 total.

- **075** — PortfolioBacktester core: `backtesting/portfolio.py` — `PortfolioBacktester` runs many instruments over a merged primary-timeframe calendar (union of store timestamps, sorted) with a single shared `TradeBook`. Cursor alignment per instrument = latest candle ≤ cursor (`MarketView.align`); a frame fires only when that instrument's aligned candle advances (frames == its own candles past warmup, not merged count). One open trade at a time; deterministic fill order = strategy priority then instrument order; pending orders fill at next candle strictly newer than the signal candle; stop/target/max-hold resolved only on the position's own instrument candles; open trade forced-closed at end of run. `BundleProtocol` gains `strategies` (priority-ordered names); `PortfolioBacktestResult.by_instrument` yields per-instrument `InstrumentBacktestResult`. 18 portfolio tests + no-readahead audit (future candles on one instrument change nothing for the other or for pre-extension trades). 1370 total.

- **074** — Trade attribution: `TradeOutcome.instrument` (canonical symbol) threaded through `submit_order`/`fill_order`/`close_trade` (default `""`, attribution additive). `TradeBook.summary["by_instrument"]` mirrors `by_strategy` breakdown. `Backtester` passes its store's canonical symbol; CLI trade log gains instrument column. 3 new tests (attribution through real backtest, by-instrument breakdown, empty default). 1352 total.

- **072** — Dukascopy provider moved to freeserv chart/json3 API: bi5 feed removed entirely; JSONP `[ms,o,h,l,c,v]` rows with forward `last_update` pagination (boundary-row dedup, range-end stop); slash-format instrument resolution (`EURUSD`→`EUR/USD`, registry preferred — bare codes get `[null]`); M1–H4/D1/W1 map to `1MIN`–`4HOUR`/`1DAY`/`1WEEK` (verified live); resilience = 60s timeout, 5 retries linear backoff, 503+`URLError` retried then new `FeedUnavailableError` (clear message), 429→`RateLimitError`, 404→`NoDataAvailableError`; page-level JSON cache; `ProviderChain` falls back on `FeedUnavailableError`. 20 dukascopy + 2 chain tests. 1349 total.

- **071** — Dukascopy D1/W1 support: `_TIMEFRAME_MINUTES` adds `D1: 1440` / `W1: 10080` (classic `_ohlcv_1440.bi5` / `_ohlcv_10080.bi5` endpoints). Fetch loop refactored into `_load_day` (cache → HTTP → parse → range-filter, `[]` on 404); intraday/D1 path unchanged; new week-granular path for W1 — week starts on the range's first Monday, probes Mon–Sun until one probe yields a bar (holiday/404 scan), clamped to range end, 7-day step between weeks. Daily/weekly bars reuse the classic `>Ifffff` layout (open/high/low/close), timestamp = probe day 00:00 UTC + file sec_offset. Live verification of the daily-bar timestamp convention remains blocked on 072; the newer `BID_candles_*` layout (open/close/low/high, int prices × point value) was cross-checked against theorycraft-trading/dukascopy and deliberately not adopted. 5 new/updated tests (all-timeframes-mapped, D1 one-candle-per-day + `_ohlcv_1440.bi5` URLs, W1 week-granular 2-weeks-2-probes, W1 intra-week 404 scan, W1 probe-day caching). 1342 total.

- **069** — Portfolio instrument file & data loading: new `data/portfolio.py` — `PortfolioSpec` + `load_portfolio(path, registry)` (YAML `instruments:` list, unknown canonical → error naming instrument+index, empty/malformed → clear error, duplicates deduped order-preserved, registry required), `InstrumentData` result, `fetch_instrument_data(provider, datastore, instrument, timeframes, start, end, base_tf)` — the store-first → native → resample loop extracted verbatim from `run_command` (label-prefixed progress, `InstrumentDataError` naming instrument when primary TF unavailable). `run --instruments <file>` loads the registry-required portfolio, fetches all instruments over identical start/end into the canonical-keyed store, aborts failed instruments with context, and reports a per-instrument data summary (portfolio execution engine lands in 070); single-`--symbol` path refactored to reuse the helper, behavior unchanged. 26 new tests (loader validation, multi-instrument fetch with per-instrument resample fallback, store reuse, CLI end-to-end). 1334 total.
- **Data time-queries** — `MarketStore.timestamp_index` (O(log n) bisect lookup of the latest candle at-or-before a timestamp, per timeframe) and `MarketStore.in_range` (inclusive `[start, end]` range slice by timestamp). `MarketView.select` reuses `timestamp_index` (was O(n) scan); new `MarketView.align(timestamp)` returns a same-timeframe view whose cursor is the latest candle ≤ timestamp (clamped to 0). Sorted-timestamp contract documented on `MarketStore`. 21 new tests. 1308 total.
- **068** — CLI unified symbol resolution: `Instrument` gains `provider_priority` (validated against known provider names, YAML round-trip, `InstrumentRegistry.get_priority`, `instruments add --provider-priority`). `run`/`fetch` load the registry (default `data/instruments.yaml`, `--registry` override), build a `ProviderChain` per instrument in declared priority order (default order otherwise), resolve `--symbol` as canonical — unknown symbol → stderr WARNING + raw passthrough. DataStore now keyed by canonical name. `ProviderChain.providers` property. 25 new tests. 1290 total.
- **067** — Confirmed pullback entry: `PullbackPatternAnalyzer` emits `PullbackFact` only on the candle following the final swing point (entry-day gate, timestamp-based, cross-TF safe) and only when that candle confirms the move (body ratio ≥ min_body_pct 0.6, close beyond swing, correct direction). Rejections surface per-candle evidence (awaiting entry candle / entry window missed / WARNING confirmation candle weak). Requires ≥2 lows + ≥2 highs so marginal swing sets no longer form patterns vacuously. `AnalysisGraph.run_with_evidence()` returns analyzer evidence for no-fact frames; `Backtester` appends it so rejected confirmations are observable. Confirmed pullback flows `PullbackSignal` → `RiskEngine` → sized `TradeCandidate` (e2e). Cross-TF unit tests (1w structure → 1d confirmation) and graph gating test. 19 new tests. 1234 total.
- **044** — Semantic model document: new `docs/semantic-model.md` — reference for the AST layer. Defines every concept (Analysis/Definition/Parameter/Expression hierarchy incl. ReferenceExpression/Capability/Provider/ProviderContract/Fact/FactKey/TimeFrame), relationships (definition→provider→capability, fact wiring via FactKey), full lifecycle (construct via builder/factories/DSL → 4-stage pipeline → TemplateGraph instantiation → AnalysisGraph execution), naming conventions and best practices, ASCII diagrams, model map to modules/backlogs. Notes Binding retired in favour of ReferenceExpression. 0 new tests (docs-only). 1239 total.
- **060** — DSL integration tests (text → graph): new `tests/test_dsl_integration.py` — 40 end-to-end golden tests over the full path DSL text → lexer → parser → pipeline (validation/registry/param-schema → choice expansion → concrete validation → graph) → `AnalysisGraph`, organised by pipeline stage (Stage0 Parse → Stage4 Graph/Config + end-to-end). Spec fixtures (single/linear-forward-ref/branching-shorthand/pullback tree/full 7-analyzer strategy with choice + literal variants/timeframe refs); parse==builder-equivalent assertion; expansion counts (single/nested/cartesian ordering/list-literal/choice-over-reference); graph edges + execution order; positioned error fixtures (unknown type, unknown/mistyped param, missing timeframe resolution, unknown reference, empty choice at parse + expansion, duplicate names, duplicate fact producer, undeclared dependency); `compile_dsl` determinism and graph isolation. 40 new tests. 1239 total (9 pre-existing failures in html_renderer/no_readahead/signal_system untouched).
- **001** — Project scaffolding and data model foundation
- **006** — Fact type system: base Fact, primitive facts (EMA/SMA/ATR/RSI/Volume), structural facts (Trend), pattern facts (Pullback). 19 tests passing.
- **003** — Parquet data loading: MarketRepository loads OHLCV from Parquet files into MarketData. Symbol listing, column validation, clear errors. 8 tests.
- **004** — MarketStore: immutable candle collection with O(1) indexed access, slicing, timestamps, symbol/timeframe metadata. 10 tests.
- **005** — MarketView: cursor-based window into MarketStore. Current candle, history window, price/volume/high/low/timestamp series, validity checks. 15 tests.
- **007** — Analyzer interface + graph: Analyzer base class with requires()/produces()/analyze(). AnalysisGraph with topological sort, cyclic dependency detection, unsatisfied dependency detection. 16 tests.
- **008** — EMA analyzer: EMAAnalyzer computes EMA over close prices from MarketView. SMA-seeded, configurable period, bullish/bearish signal evidence. 12 tests.
- **009** — ATR analyzer: ATRAnalyzer computes True Range and ATR from candle data. SMA-seeded with Wilder smoothing, configurable period, pct-of-price evidence. 12 tests.
- **010** — Trend analyzer: TrendAnalyzer computes fast/slow EMA comparison for direction (BULLISH/BEARISH/NEUTRAL) and strength (spread normalized by ATR or price). 15 tests.
- **011** — Pullback detector: PullbackDetector consumes TrendFact + ATRFact to detect retracement within trend. Swing detection with fallback to extremes, ATR-denominated retracement depth, DETECTED/INVALIDATED status. 17 tests.
- **012** — Analysis frame storage: AnalysisFrame (frozen dataclass capturing timestamp, candle, facts, evidence, annotations, diagnostics) and FrameStore (in-memory collection with append/getitem/slice/by_timestamp, Parquet round-trip serialization). 10 tests.
- **013** — Evidence model: EvidenceEntry (text, level, source, annotation_hint), EvidenceLevel enum (INFO/SIGNAL/WARNING), EvidenceCollector (add, entries, by_level, summary). Structured evidence throughout analyzers, facts, and frames. 155 total tests.
- **014** — Backtesting replay loop: Backtester iterates MarketStore, creates MarketView per step, runs AnalysisGraph, stores AnalysisFrame. Progress callback, edge cases (exact window, zero frames). 13 tests. 168 total.
- **015** — HTML visualization: HTMLRenderer produces self-contained HTML with TradingView lightweight-charts. Candlestick chart, EMA overlays, ATR panel, pullback markers, evidence on crosshair move. 17 tests. 185 total.
- **002** — External data fetcher: DataProvider interface, YahooProvider with yfinance, ProviderChain for fallback, CLI command. 7 tests. 192 total.
- **016** — Strategy config loader: YAML→StrategyConfig with AnalyzerConfig/SignalConfig/RiskConfig. Analyzer registry, validation, build_analyzers. 21 tests. 213 total.
- **017** — Swing structure analyzer: SwingStructureAnalyzer detects swing highs/lows from MarketView. SwingFact with ordered alternating swings, ATR-based separation filter, configurable lookback. 18 tests. 231 total.
- **018** — Four-swing pullback detector: FourSwingPullbackDetector consumes SwingFact+TrendFact+ATRFact, validates HH/HL/HH/HL and LL/LH/LL/LH patterns, linearity filter, confirmation candle check. Enhanced PullbackFact with swing_pattern, deviation_pct, confirmation_strength. 30 tests. 261 total.
- **019** — S/R analyzer: SupportResistanceAnalyzer consumes SwingFact+ATRFact, clusters swings within ATR tolerance, classifies as support/resistance by current price. SRFact with levels sorted by price, strength as touch count. 15 tests. 276 total. **Registry fix**: Added SupportResistanceAnalyzer to ANALYZER_TYPES in loader.py.
- **020** — Signal system: TradeSignal dataclass, Signal ABC, PullbackSignal evaluates PullbackFact+TrendFact+ATRFact for entry signals. Strategy class builds graph+signals from config, evaluates at each frame. 23 tests. 299 total.
- **021** — Risk layer: RiskEngine consumes TradeSignal+ATRFact+SwingFact+SRFact to produce TradeCandidate with entry (slippage-adjusted), stop (swing-based), target (RR-based), position size (balance × risk_pct). S/R crossing rejection, max stop distance clamp, configurable RR range. TradeCandidate dataclass. 22 tests. 321 total.
- **022** — Strategy-aware backtester: TradeBook tracks trades with P&L, win/loss, drawdown. StrategyBundle merges multiple strategy graphs with dedup. Backtester integrates signals+risk engine, fills orders at next open, resolves stop/target. 17 tests. 325 total.
- **023** — No-readahead audit: 12 regression tests proving no future data leakage. Frame independence, MarketView boundary, analyzer/signal/risk isolation, TradeBook resolution (fill at N+1 open), full backtest determinism, single-trade constraint. Fixed StrategyBundle._merge_graphs sorting bug. 12 tests. 337 total.
- **024** — Interactive strategy visualization: RenderContext dataclass, InteractiveRenderer generates self-contained HTML with frame stepper UI. Trade markers (entry/exit), S/R level price lines, trade zones (entry/stop/target), frame info panel (indicators, patterns, S/R, trade status), trade timeline bars, summary stats, keyboard navigation (arrows+space), playback with speed control. 35 tests. 372 total.
- Trade performance metrics: profit_factor, avg_win, avg_loss, expectancy, gross_profit, gross_loss added to TradeBook.summary. Interactive viz summary bar updated with PF, Avg W, Avg L. 5 new tests. 377 total.
- **025** — Backtest CLI: `run` subcommand in `cli.py` with --strategy, --symbol, --start, --end, --interval, --output, --balance, --max-hold-days. Loads strategy YAML, fetches data, runs backtest, prints stats (P&L, win rate, drawdown, profit factor, expectancy, per-strategy breakdown), generates HTML chart. 377 tests, mypy clean.
- TradeBook fix: total_trades now includes breakeven trades (was win+loss only). Added breakeven_count, closed_count properties. Strategy breakdown counts breakevens. 14 new tests. 391 total.
- **026** — JS template extraction: Moved inline JS from interactive.py into interactive.js file with `null; // @data:PLACEHOLDER` markers. Python renderer reads file, replaces markers with JSON data via string replace, injects into HTML. JS validates with `node --check`. 391 total.
- **027** — HTML output validation tests: 15 tests validating generated HTML — JS syntax via node --check, data constants present, DOM elements, non-empty arrays, brace balance, smoke tests. 406 total.
- Testing: added 10 edge case tests for strategy config loader (non-mapping items, non-list requires, non-mapping params/rules). loader.py coverage 90%→100%. 438 total.
- Testing: graph.py (71%→100%) — duplicate-producer error, AnalyzerRegistry (register/build/resolve). backtester.py (87%→100%) — signal eval path, close remaining trade. Introduced BundleProtocol for duck-typed bundle param. 12 new tests. 450 total.
- Testing: cli.py (0%→99%) — 14 tests covering fetch/run commands, error paths, strategy loading, provider failures, date defaults, trade log display, by-strategy breakdown, parser validation. Overall coverage 91%→97%. 464 total.
- Testing: swing.py (94%→100%) — consecutive same-type swing replacement. base.py (88%→100%) — default instance_key. risk.py — swing fallback paths, size/reward calc. 9 new tests. 478 total.
- **028** — Swing point markers: swing highs/lows rendered as small arrow markers on candlestick chart. Orange down-arrows above bar for highs, blue up-arrows below bar for lows. Per-frame update, read-ahead safe. 1 new test. 479 total.
- Testing: four_swing_pullback.py (95%→98%) — 6 edge case tests for linearity out-of-bounds/same-index/flat-candle skips, confirmation flat-candle body%, volume ratio short data/zero volume. risk.py — documented dead code (stop_distance<=0 unreachable, S/R crossing rejection redundant with _find_valid_rr). 9 new tests. 488 total.
- **029** — Pullback pattern zigzag line: `_extract_facts_per_frame` now includes `swing_pattern_indices` by correlating pullback prices with SwingFact swings. JS creates two line series (bullish green, bearish red) and draws zigzag connecting swing points when pullback detected/confirmed. 3 new tests. 490 total.
- **030** — S/R horizontal lines with touch-based thickness: min_touches param on RenderContext (default 2), levels below threshold filtered from chart and info panel. Tiered line width (2=1px, 3+=2px, 5+=3px). MIN_TOUCHES injected into JS template. 4 new tests. 494 total.
- **035** — Future candle visibility: hide/dim/show toggle for candles beyond current frame cursor. Default hide (most honest backtest view), dim mode (30% opacity), show mode (original). V key or button toggle. Auto-scroll to current candle, disabled on manual pan/zoom. Info panel shows frame position (X/Y) and remaining candles. 497 total.
- **034** — Frame stepper candle visibility: auto-scroll works in all visibility modes (was hide-only). Crosshair snaps to current candle on frame update. Smooth scroll animation via scrollPosition for dim/show modes. Auto-scroll toggle button (A key or click), visual feedback on manual pan disable. 6 new tests. 502 total.
- Expectancy stat added to interactive visualization summary bar. Fixed bearish trade entry price line color (was always green). 1 new test. 502 total.
- **036** — Volume histogram: 80px panel between chart and ATR with color-coded bars (green=up, red=down). Respects hide/dim/show candle visibility. Three-way time scale sync (volume↔chart↔ATR). Resize handler. 5 new tests. 507 total.
- First/Last frame buttons with Home/End keyboard shortcuts added to interactive viz. Jump to start/end of frame sequence. 506 total.
- **036** — AST model: BaseNode, Parameter, Binding, Definition, Analysis frozen dataclasses. Immutable semantic model for analysis definitions. 24 tests. 531 total.
- **037** — Builder API: AnalysisBuilder fluent class with define/with_param/bind/build chainable methods. _DefinitionBuilder proxies parent methods for seamless chaining. Eager validation (duplicate names, unknown references). 13 tests. 544 total.
- **039** — AST serialization: JSON serialization/deserialization for AST. to_dict/from_dict, to_json/from_json, pretty-print, round-trip, error handling for malformed/missing fields. 33 tests. 607 total.
- **040** — Compiler adapter: ASTCompiler converts Analysis → StrategyConfig and AnalysisGraph. to_config extracts analyzer/signal/risk configs from AST definitions. compile builds AnalysisGraph reusing existing build_analyzers path. Signal requires derived from bindings. 16 tests. 623 total.
- Scroll animation fix: removed `futureVisibility === 'hide'` early return in scrollToFrame. All visibility modes now use scrollToPosition with 150ms ease-out animation for consistent smooth scrolling.
- **041** — Capability/Provider/Definition: added Capability+Provider frozen dataclasses, removed type/impl from Definition (replaced with provider), added providers to Analysis. Builder auto-creates providers from old-style define(). Compiler resolves provider categories. Validation checks provider refs. Serialization includes providers. 132 AST tests pass. 638 total.
- **043** — Provider Registry: ProviderRegistry with register/resolve/list_providers. Supports Provider objects, class-based registration, and decorator syntax. Default registry pre-populated with 8 built-in providers. 26 tests. 664 total.
- **033** — Instrument Registry: Instrument dataclass (canonical, asset_class, description, providers), InstrumentRegistry (load/save YAML, lookup by canonical, reverse resolve, CLI instruments list/add). YahooProvider supports optional registry for symbol resolution. 16 tests. 680 total.
- **046** — Snapshot tests for compiled analyses: single/linear/branching/full strategy AST + graph snapshots. `--update-snapshots` flag, JSON snapshots in `tests/snapshots/`. 10 tests. 713 total.
- **034 fix** — scrollToFrame now uses `candleSeries.data()` instead of CANDLES for index lookup, clamps targetPos ≥ 0, widens scroll threshold to 15% of visible bars. Prevents broken auto-scroll when future candles hidden.
- Viz: breakeven count displayed in summary bar. JS tracks `breakevens` in `updateSummary()`, BE: stat in HTML template. `s-trades` shows total closed (incl. breakevens). Win rate uses win+loss only. 715 total.
- Viz: OHLCV values in info panel. Replaced single Close with Open/High/Low/Close/Volume rows. 1 test. 716 total.
- Fix: candle highlight now uses `candleSeries.update()` per-frame to set yellow border/wick on current candle. Replaced broken `LineSeries.setData()` approach (duplicate timestamps deduped by lightweight-charts, only showed one dot). 1 test. 716 total.
- **032** — Dukascopy data provider: DukascopyProvider fetches BI5 from Dukascopy datafeed, decompresses zlib, parses binary format, caches locally. 12 tests. 727 total.
- **035 fix** — Dim mode sets borderColor/wickColor on future candles (was leaving green/red borders). Show mode uses `_visibilityNeedsReset` flag to call setData only on toggle, not every frame. 3 tests. 730 total.
- **046** — Multi-timeframe data fetching: resample module (H1→D1, H1→H4, D1→W1, etc.), MarketStore multi-series storage (`dict[Timeframe, MarketData]`), StrategyConfig.timeframes field, CLI fetches all required TFs with native+resample fallback. 8 new tests. 767 total.
- **045** — Multi-resolution MarketView: FactKey +timeframe, Fact +visible_on, MarketView.select(timeframe) with cursor alignment, Analyzer.timeframe property, AnalysisGraph per-analyzer view selection, interactive resolution switcher. 30 new tests. 797 total.
- **066** — Persistent data store: DataStore with Parquet-backed (symbol, timeframe) persistence, coverage/has/missing_ranges queries, atomic writes, UTC-normalized timestamps, fetch_or_get serving cached ranges and fetching only gaps. CLI run uses store before fetching (--data-dir, default $MARKETATLAS_DATA_DIR or ~/.cache/marketatlas/data), resampled data cached too. 23 new tests. 817 total.
- **047** — Expression hierarchy: `Expression` base + `LiteralExpression(value)` in new `expressions.py`. `Parameter.value` typed `Expression`. Builder/registry wrap raw values in `LiteralExpression` (transparent equality keeps `.value == 20` working). Literal serializes as raw JSON value (format unchanged); non-literal param serialization and `_ast_to_config` raise clear errors naming definition+param. 22 new tests. 839 total.
- **048** — ChoiceExpression node: `ChoiceExpression(values: tuple[Expression, ...])` + `Choice(...)` factory (wraps raw members as literals, passes Expression/nested-choice through). Raw list param stays a plain literal — never implicitly a choice. Structured serialization `{\"expr\": \"choice\", \"values\": [...]}` distinct from raw list literals; round-trip incl. nested choices. Builder `with_param(\"period\", Choice([50, 100]))` keeps expression. Defensive `CompilationError` in `_ast_to_config` when an unexpanded choice reaches config generation, naming definition+param. 22 new tests. 861 total.
- **049** — Deep clone: `clone(node)` + `clone_expression(expr)` in new `clone.py`. Explicit recursive clone for Analysis/Definition/Parameter/Binding/Provider/Capability/Expression; metadata dicts copied, literal payloads deep-copied (mutable list/dict values never shared), ChoiceExpression subtrees cloned recursively. Typed overloads guarantee same static type as input. Pure-literal clone round-trips equal; id/metadata preserved. 16 new tests. 877 total.
- **050** — Template expansion pass: `expand(analysis) -> tuple[Analysis, ...]` in pipeline.py. ChoiceExpression params replaced by flattened leaves (nested choices recurse); product across params/definitions in declaration order (rightmost varies fastest); list literals never expand; empty choice → CompilationError naming def+param; output guaranteed choice-free; built from clone(049), input never mutated; convergent duplicates deferred to stage-3 validation (051/054). 22 new tests. 899 total.
- **051** — Compiler pipeline stages: restructured into four explicit stages — AST validation → template expansion (TemplateExpansionPass, fork via run_all) → concrete AST validation (ConcreteValidationPass: choice-free + duplicate-definition checks) → graph compilation. Removed redundant DefinitionExpansionPass; default-param merge single owner = RegistryResolutionPass. `Pipeline.expand() -> tuple[Analysis, ...]` and `compile_all() -> tuple[AnalysisGraph, ...]` for multi-output; `Pipeline.run`/`ASTCompiler.compile` single-concrete path unchanged; `ASTCompiler.expand`/`compile_all` entry points. Fixed stale `compute_ema` capability in a registry test. 17 new tests. 916 total.
- **052** — Provider param schema validation: new `param_schema.py` (`ParamSpec`, `derive_param_schema` via `inspect.signature`+`get_type_hints`, `type_compatible` — int→float widening, union/Any/generic accept-all), schemas registry-held keyed by provider name+capability and auto-derived on `register()`. New stage-1 pass `ParamValidationPass` (post-registry-resolution, pre-expansion) rejects unknown params, missing required params, wrong-typed literal values; `ChoiceExpression` leaves checked individually. No false positives on opaque/unregistered providers. Wired into `ASTCompiler._pipeline`. 30 new tests. 946 total.
- **053** — Provider construction API: `constructors.py` with `EMA(period=20)`/`Choice([50,100])` style factories for all 9 built-in capabilities + generic `construct()`. Resolves capability via default registry, applies registered default params (explicit kwargs override), validates params against 052 schema at construction (unknown kwarg / missing required / wrong type / per-choice-leaf type → `ProviderConstructionError` naming provider). `build_analysis()` composes definitions into self-contained `Analysis` (providers populated, deduped, serialization round-trip) — literal-only compiles via `ASTCompiler.compile`, choices expand/compile_all. Moved `_choice_leaves` → public `choice_leaves` in expressions.py. 29 new tests. 975 total.
- **054** — Compiler golden tests: new `test_ast_golden_expansion.py` — explicit inline expected outputs (no snapshots) pinning choice expansion through `Pipeline.expand` (stages 1-3: validation, registry resolution, param validation, expansion, concrete validation). Full-AST equality + `to_dict` golden for single/nested/multi-choice (same-def swingstructure, cross-def ema×atr cartesian ordering), list literal vs choice (opaque provider), empty-choice errors naming def+param, stage-3 duplicate-definition guard, stage-1-first duplicate ordering, determinism. 13 new tests. 988 total.
- **056** — DSL lexer: new `lexer.py` — single-pass maximal-munch scanner producing ordered `Token`s with 1-based `SourcePosition` (line/col, shared position model for 059). Token set from 055 grammar: structural (`:=`/`{`/`}`/`:`/`,`/`[`/`]`/`<`/`|`/`>`), `IDENT`, `INT` vs `FLOAT` (lexeme + parsed `value`), `STRING` with escapes. Whitespace-insensitive; `//` line comments skipped. Choice brackets balance-checked lexically (`|`/`>` outside a choice and unterminated `<` → positioned `DslSyntaxError`; lone `=` → truncated `:=`; illegal chars → error). Round-trip property: lexeme concat reproduces source modulo whitespace/comments (string inner whitespace preserved). 36 new tests. 1024 total.
- **058** — Provider contract metadata: `ProviderContract(inputs, outputs)` frozen dataclass derived at registration (`_register_metadata`) from runtime `requires()`/`produces()` via default construction, `FactKey` reduced to `.name`; unconstructable/opaque classes get empty contract. Keyed by provider name AND capability; `registry.contract(key)` / `register_contract()` / `capabilities()` (sorted). This is the fact-level contract the 057 shorthand disambiguation consumes. 12 new tests. 1030 total.
- **057** — DSL parser: new `parser.py` — `parse(source, *, name, version, registry)` producing self-contained `Analysis` via `build_analysis`. Grammar `name := Type { fields }`: Type = registry capability key (unknown Type lists available capabilities); `name: expr` → Parameter (int/float/string/bool/null literals, negatives, lists as plain literals, choices `[`..`|`..`]`-style preserved intact); `name: ref-ident` → Binding `(source, output, target, input)`, `output = input = field name` (deliberately not contract-output inference — parameterised fact names like `ema_50` would break); shorthand `name,` → Binding iff field ∈ provider contract inputs (058); `true`/`false`/`null` reserved. Forward refs supported (raw bindings resolved post-parse). Duplicate def names, unknown source definitions, stray tokens, missing braces → positioned `DslParseError`. Full literal strategy compiles via `ASTCompiler` to 7-analyzer graph. 47 new tests. 1077 total.
- **059** — Positioned diagnostics: `Diagnostic` gained optional `position` (backlog 038 unchanged). New `diagnostics.py` — `SourceMap` (def/param name → `SourcePosition`, produced by new `parse_with_positions`), `with_position`, compact `format_diagnostic` (`line:col: <severity>: <message>`), `format_errors` (multi-error block), `render_snippet` (offending line + caret). Pipeline stage-1/3 passes accept optional `SourceMap` (`ValidationPass`, `ParamValidationPass`, `ConcreteValidationPass`) and attach positions to `CompilationError.errors` "where source-mapped"; `expand()`/`TemplateExpansionPass` position the empty-choice error; `Pipeline(source_map)` threads it to stage 2/3. `ASTCompiler.compile_dsl(source)` end-to-end: text → positioned compile, one graph per concrete AST. 37 new tests. 1114 total.
- **061** — Timeframe declaration on analysis nodes: `Definition.timeframe: Timeframe | None` (None = strategy base), `Analysis.timeframes: tuple[str, ...]` (derived via `derive_timeframes` — base `timeframes[0]` from analysis base TF or first explicit def TF, deduped). Builder `.with_timeframe()` on def builder and `AnalysisBuilder` (invalid string → ValueError). Serialization round-trip (def `timeframe` + analysis `timeframes`, invalid → ValueError). Clone preserves both. `_ast_to_config` emits `AnalyzerConfig.timeframe` + `StrategyConfig.timeframes`; graph run selects per-analyzer views (verified with D1+W1 store). Validation rejects non-`Timeframe` def timeframe and invalid analysis TF strings. DSL reserved `timeframe` field (`ema := ema { timeframe: "1h" }`) — quoted-string only, invalid → positioned `DslParseError`, unquoted/bare → error; parser result equals builder equivalent; `build_analysis` derives timeframes. 38 new tests. 1152 total.
- **062** — Cross-timeframe references: `field: producer` on an analyzer resolves to producer's declared timeframe and injects a `bindings` override (`name@timeframe` when source TF ≠ consumer TF, bare name otherwise). `Analyzer._make_key` applies override (skipping instance key so `produces()` stays stable) and parses `@tf` suffix into FactKey. `_check_fact_declared` validates reference outputs by instantiating target with literal params + source TF and checking `produces()` (precise for parameterised names like `ema_50`). Signal `requires` carries `name@source_tf` for non-base-TF refs; single-TF entries stay bare (046 behavior preserved). Undeclared cross-TF dependency fails at graph construction (`UnsatisfiedDependencyError`). 17 new tests. 1169 total.
- **063** — Instrument runtime expansion: new `instrument.py` — `TemplateGraph` (instrument-neutral recipe: concrete `Analysis` + `StrategyConfig`, reference `graph`, cheap repeatable `instantiate`/`instantiate_all` rebuilding fresh isolated `AnalysisGraph`s), `InstrumentGraph` (instrument identity + isolated graph, `canonical`), `backtest_template` (per-instrument isolated backtests, empty list → error). `Pipeline.compile_template`/`compile_templates` (multi-output for choice templates, single raises), `ASTCompiler` entry points, exports. 13 new tests. 1182 total.
- **065** — Data requirements inference: new `requirements.py` — `required_timeframes` (declared `Analysis.timeframes` → deduped `Timeframe` tuple, declaration order), `DataRequirement` (instrument × timeframe × date range for coverage checks), `required_data` (cartesian instrument×TF product, dedup by canonical, deterministic, empty-instruments error), `ensure_required_data` (DataStore.fetch_or_get gap-only fetch integration). `Analysis.required_timeframes()` method; `TemplateGraph.required_timeframes()`/`required_data()` deriving from compiled `StrategyConfig.timeframes` (declared set or base default). Exports in `__init__`. 23 new tests. 1199 total (9 pre-existing failures in html_renderer/no_readahead/signal_system untouched).
- **064** — Market groups: cross-instrument nodes. `SCOPE_INSTRUMENT`/`SCOPE_GROUP` scope constants + `Definition.scope` (default `instrument`), `is_group_scope`. New `SpanningReferenceExpression(ReferenceExpression)` (spans every group member). DSL: contextual `group` keyword (`group name := P { field: source* }`, `group` still legal as a definition name) + `*` reference suffix; group-scoped shorthand `field,` auto-expands to spanning. Builder `.with_scope`/`.with_spanning_reference`. Serialization/clone round-trip scope + spanning. Validation: group defs must be analyzers; spanning only on group defs; spanning targets must be per-instrument non-TimeFrame defs; per-instrument defs cannot reference group defs (cross-group); group defs must use spanning refs for fact producers (plain timeframe refs still legal). `Pipeline.compile_template` emits per-instrument + group `StrategyConfig`s; `_ast_to_config` scope-filtered, unfiltered `compile` rejects group defs. Spanning references resolve member-agnostic (`name@source_tf or '1d'`). `TemplateGraph.instantiate_group(group, members)` → frozen `GroupGraph` (N isolated member `InstrumentGraph`s + 1 `GroupNodeGraph` whose lenient topo-sort tolerates member-namespaced external inputs `{canonical}/name@tf`); `_build_group_analyzers` injects `members` at instantiation. Group nodes data-free. docs/dsl.md grammar/design-decisions updated (group no longer deferred). 29 new tests. 1244 total.
- **055** — DSL grammar spec document: new `docs/dsl.md` — lexical + syntactic EBNF, semantics (definition/parameter/reference/shorthand disambiguation via provider contract inputs, fact-name-is-field-name rule, expression hierarchy, timeframes, template expansion), all design decisions resolved with rationale (capability-key Type mapping, deferred provider-selection choice, deferred group scoping), DSL→AST mapping table, draft-spec examples transcribed and annotated (incl. canonical 7-analyzer strategy), compiler-pipeline relationship (text → lexer 056 → parser 057 → template AST → stages 1-3 → 4 graph), error model, open questions. Pinned from implemented 056-059/061-062 code. 0 new tests (docs-only). 1199 total.